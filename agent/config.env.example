# ---------------------------------------------
# VPS Insight — Agent Configuration
# ---------------------------------------------
# This is a TEMPLATE file.
# The real config is created automatically by install.sh at:
#   /etc/vps-insight/config.env
#
# Do NOT edit this file for a live install.
# ---------------------------------------------

# Base URL of your Cloudflare Worker
# No trailing slash
WORKER_URL=https://vps-insight-prod.example.workers.dev

# API token (must match the Worker secret)
API_TOKEN=replace-with-your-long-random-token

# Unique identifier for this VPS
# Used only for display & metadata
VPS_ID=vps-prod-1
