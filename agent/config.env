# VPS Insight — Runtime Configuration
# This file is read by the agent at runtime.

# Base URL of your Cloudflare Worker (NO trailing slash)
WORKER_URL=https://your-worker-name.workers.dev

# API token configured in the Worker
API_TOKEN=replace-with-your-token

# Stable identifier for this VPS (used as KV prefix)
VPS_ID=gbj-prod-1
